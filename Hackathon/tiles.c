//functions in this should be called using the i/j parameters of a tile in a room and the pointer to the string tile[i][j].type
//unfinished!!
void make_desk(int i, int j){
	G_rgb(.5,.15,0);
	G_fill_rectangle(j*50, 800 - (i+1)*50, 50, 30);
	G_fill_rectangle(j*50, 800 - (i+1)*)
}
